global:
  scrape_interval: 5s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "devops-monitoring-app"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["monitoring-app:5000"]
